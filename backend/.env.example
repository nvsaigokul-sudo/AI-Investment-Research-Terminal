PORT=3001
GEMINI_API_KEY=
# Optional: Set this to use a PostgreSQL database. If omitted, the app falls back to a local SQLite database (dev.db).
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
